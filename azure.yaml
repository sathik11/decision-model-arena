# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

requiredVersions:
  azd: '>=1.27.1'
  extensions:
    azure.ai.agents: '>=1.0.0-beta.9'
name: critical-asset-triage
services:
  ai-project:
    host: azure.ai.project
    deployments:
      - name: gpt-5.4-mini
        model:
          format: OpenAI
          name: gpt-5.4-mini
          version: '2026-03-17'
        sku:
          name: GlobalStandard
          capacity: 10
  critical-asset-triage:
    host: azure.ai.agent
    project: .
    language: python
    codeConfiguration:
      runtime: python_3_13
      entryPoint: hosted_agent.py
    uses:
      - ai-project
    kind: hosted
    name: critical-asset-triage
    displayName: Critical Asset Triage
    description: Foundry agent with a confidence-gated typed decision tool for critical asset incidents.
    protocols:
      - protocol: responses
        version: 2.0.0
    env:
      AZURE_AI_MODEL_DEPLOYMENT_NAME: ${AZURE_AI_MODEL_DEPLOYMENT_NAME}
      DECISION_ENGINE: ${DECISION_ENGINE=auto}
      TYPESAFE_API_KEY: ${TYPESAFE_API_KEY}
      TYPESAFE_MODEL: ${TYPESAFE_MODEL=jev-latest}
      LAYA_MODEL: ${LAYA_MODEL=convaiinnovations/laya-typed-decisions}
      LAYA_SUBFOLDER: ${LAYA_SUBFOLDER=}
    container:
      resources:
        cpu: '1'
        memory: 2Gi
infra:
  provider: microsoft.foundry
